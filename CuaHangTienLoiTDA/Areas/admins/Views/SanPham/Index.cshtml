@model IEnumerable<CuaHangTienLoiTDA.Models.SanPham>

@{
    ViewData["Title"] = "Quản lý Sản phẩm";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var sample = Model.FirstOrDefault() ?? new CuaHangTienLoiTDA.Models.SanPham();
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="fw-bold text-primary"><i class="fa-solid fa-box"></i> Quản lý Sản phẩm</h2>
        <a class="btn btn-success" asp-action="Create">
            <i class="fa-solid fa-plus"></i> Thêm Sản phẩm
        </a>
    </div>

    <div class="table-responsive shadow-sm rounded-3">
        <table class="table table-striped table-hover align-middle mb-0">
            <thead class="table-primary text-center">
                <tr>
                    <th>@Html.DisplayNameFor(m => sample.TenSP)</th>
                    <th>@Html.DisplayNameFor(m => sample.Gia)</th>
                    <th>@Html.DisplayNameFor(m => sample.SoLuong)</th>
                    <th>@Html.DisplayNameFor(m => sample.HinhAnh)</th>
                    <th>@Html.DisplayNameFor(m => sample.MoTa)</th>
                    <th>Danh mục</th>
                    <th style="width: 180px;">Hành động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.TenSP</td>
                        <td>@String.Format("{0:N0} đ", item.Gia)</td>
                        <td class="text-center">@item.SoLuong</td>
                        <td>
                            @if (!string.IsNullOrEmpty(item.HinhAnh))
                            {
                                <img src="~/uploads/@item.HinhAnh" alt="Ảnh sản phẩm" width="60" height="60" class="rounded border" />
                            }
                            else
                            {
                                <span class="text-muted">Chưa có</span>
                            }
                        </td>
                        <td>@item.MoTa</td>
                        <td>@item.MaDMNavigation?.TenDM</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.MaSP" class="btn btn-sm btn-warning me-1">
                                <i class="fa-solid fa-pen"></i> Sửa
                            </a>
                            <a asp-action="Details" asp-route-id="@item.MaSP" class="btn btn-sm btn-info text-white me-1">
                                <i class="fa-solid fa-eye"></i> Chi tiết
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.MaSP" class="btn btn-sm btn-danger">
                                <i class="fa-solid fa-trash"></i> Xóa
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<p>
    <a asp-area="admins" asp-controller="Dashboard" asp-action="Index" class="btn btn-secondary">
        ⬅ Quay về trang chủ
    </a>
</p>

<style>
    .table-responsive {
        border: 1px solid #dee2e6;
        border-radius: 8px;
        overflow-x: auto;
    }

    .table th {
        background: #0d6efd;
        color: #fff;
        text-align: center;
    }

    .table td {
        text-align: center;
        vertical-align: middle;
        padding: 0.5rem;
    }

    .table img {
        width: 60px;
        height: 60px;
        object-fit: cover;
        border: 1px solid #dee2e6;
        border-radius: 6px;
    }

    .table-hover tbody tr:hover {
        background: #f1f3f5;
    }

    .btn-sm i {
        margin-right: 4px;
    }
</style>
